# =================================================================
# Frontend Environment Variables Specification (Vite)
# =================================================================

# Backend API base URL (Must include /api/v1)
VITE_API_BASE_URL=http://localhost:8000/api/v1

# Pipecat WebSocket streaming endpoint path
VITE_PIPECAT_ENDPOINT=/stream/connect
